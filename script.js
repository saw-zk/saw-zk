// メッセージを書き換える関数
function changeMessage() { //関数（処理のまとまり）を定義
    const messageElement = document.getElementById("message"); //HTML内のid="message" の要素を取得
    messageElement.textContent = "学びながら成長中です！"; //要素の中身の文字を変更
  }
  